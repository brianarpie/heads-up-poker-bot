(function() {

  "use strict";

  var app = angular.module("HeadsupPokerBotApp");

  app.controller("TableController", [
    "$scope",
    function($scope) {
      // var box = new PIXI.Rectangle(100, 100, 200, 200);
      // $scope.stage.addChild(box);
      // $scope.renderer.render($scope.stage);
    }
  ]);

})();
